<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="uz.icerbersoft.mobilenews">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="uz.icerbersoft.mobilenews.presentation.application.Application"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="allowBackup"
        android:theme="@style/Theme.MobileNews">

        <activity
            android:name="uz.icerbersoft.mobilenews.presentation.global.GlobalActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileNews.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>