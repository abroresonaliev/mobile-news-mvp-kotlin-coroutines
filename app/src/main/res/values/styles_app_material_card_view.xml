<resources>

    <style name="Theme.MobileNews.MaterialCardView" parent="android:style/Widget"/>

    <style name="Theme.MobileNews.MaterialCardView.Default">
        <item name="cardCornerRadius">@dimen/_8sdp</item>
        <item name="cardElevation">@dimen/_2sdp</item>
        <item name="cardBackgroundColor">@color/color_white</item>
    </style>

</resources>
